{
  "name": "facet-private-demo",
  "private": true,
  "scripts": {
    "postinstall": "cd adapter && npm install --include=dev",
    "build": "cd adapter && npm run build"
  },
  "engines": {
    "node": "20.x"
  }
}
