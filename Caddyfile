{$CADDY_HOSTS} {
    tls /etc/caddy/certs/origin.crt /etc/caddy/certs/origin.key

    reverse_proxy adapter:8546 {
        header_up X-Forwarded-Proto https
    }

    log {
        output stdout
        format console
    }
}
