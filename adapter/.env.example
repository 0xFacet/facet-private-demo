# Sepolia RPC endpoint
L1_RPC_URL=https://sepolia.infura.io/v3/YOUR_API_KEY

# Deployed contract addresses (update after deployment)
PRIVACY_POOL_ADDRESS=0x...
REGISTRY_ADDRESS=0x...

# Relayer wallet private key (must have Sepolia ETH for gas)
RELAYER_PRIVATE_KEY=0x...

# Block number when contracts were deployed (for efficient sync)
DEPLOY_BLOCK=0

# Optional: RPC port (default: 8546)
# RPC_PORT=8546
